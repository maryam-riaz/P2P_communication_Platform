{
  "name": "sosify-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android": "pnpm --filter mobile expo run:android",
    "ios": "pnpm --filter mobile expo run:ios",
    "start": "pnpm --filter mobile expo start",
    "web": "pnpm --filter mobile expo start --web"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "expo": "~54.0.35",
    "react": "19.1.0",
    "react-native": "0.81.5"
  }
}
